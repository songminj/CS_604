# Chapter 12. 데이터베이스 응용 기술
## 1. 객체지향 데이터베이스
### 1. 객체지향 데이터 모델
- 객체와 객체 식별자, 속성과 메서드, 클래스, 클래스 계층 및 상속, 복합 객체 등을 지원하는 객체지향 개념에 기반을 둔 데이터 모델
#### 객체와 객체 식별자
 - 객체: 현실 세계에 존재하는 개체를 추상적으로 표현한 것
 - 객체 식별자: 시스템 전체에서 유일하게 식별될 수 있게 함
#### 속성과 메서드
 - 속성: 해당 객체의 상태를 나타냄
	 - 관계 데이터 모델과 같은 의미지만 여기서는 속성 값을 여러개 가질 수 있음
 - 메서드: 객체의 상태를 조작함
	 - 속성 값을 검색, 추가, 삭제, 수정할 때 주로 사용
 - 메시지: 특정 객체의 속성과 메서드에 접근할 떄 사용
	 - 객체에 접근하기 위한 공용 인터페이스 역할 담당
#### 클래스
- 클래스: 속성과 메서드를 공유하는 유사한 성질의 객체들을 하나로 그룹화한 것
	- 클래스 인스턴스, 객체 인스턴스라고도 함
- 클래스 내부에 객체를 위한 데이터 구조와 메서드 구현에 관한 세부 사항 기술
#### 클래스 계층과 상속
- 클래스를 단계적으로 세분화하면 클래스 간의 계층 관계가 발생하여 클래스 계층이 형성됨
	- 상위, 하위로 나뉨
	- IS-A 관계 성립(A는 B다)
- 상속: 상위클래스의 속성과 메서드를 자신의 모든 하위클래스에 물려주는 개념
#### 복합 객체
- 시스템에서 기본으로 제공하지 않는 사용자 정의 클래스를 도메인으로 하는 속성을 가진 객체를 복합 객체라고 함
	- 해당 클래스에 속하는 객체 인스턴스의 객체 식별자를 값으로 가지게 됨
	- 속성 값으로 다른 객체 참조
	- Is-Part-Of 관계가 있는 객체 표현에 사용(전체와 부분으로 이루어진 포함 관계)
### 2. 객체지향 질의 모델
- 객체지향 데이터베이스에서는 질의 대상이 클래스, 질의 결과는 클래스에 속하는 객체 집합
- 단일 오퍼랜드: 클래스 하나 또는 클래스 하나와 해당 클래스의 하위 클래스 전체를 대상으로 하는 질의
- 다중 오퍼랜드: 여러 클래스를 대상으로 하는 질의
## 2. 객체관계 데이터베이스
- 객체지향 개념과 관계 데이터 모델의 개념을 통합한 것
	- SQL 표준
- 객체지향 DB는 객체지향 프로그래밍 개념에 기반을 두고 데이터베이스의 기능을 추가하는 데 목적을 둠
- 객체관계 DB는 관계 데이터베이스에 기반을 두고 사용자가 더 풍부한 데이터 타입을 추가할 수 있도록 하는 데 목적을 둠
## 3. 분산 데이터베이스 시스템
- 중앙 집중식 데이터베이스 시스템: 데이터베이스 시스템을 물리적으로 한 장소에 설치하여 운영
- 분산 데이터베이스 시스템: 물리적으로 분산된 시스템을 네트워크로 연결해 논리적으로는 하나의 중앙 집중식으로 사용
### 1. 분산 데이터베이스 시스템의 구성
- 분산 처리기: 지역별로 필요한 데이터를 처리할 수 있는 지역 컴퓨터
	- 지역에서 운영하는 데이터베이스를 자체적으로 관리할 수 있는 DBMS를 가짐
- 분산 데이터베이스: 물리적으로 분산된 지역 데이터베이스
	- 분산 처리기와 함께 지역의 데이터 처리 지원
- 통신 네트워크: 네트워크를 통해 자원 공유
	- 통신 네트워크에 있는 모든 분산 처리기는 특정 통신규약에 따라 데이터를 전송함
### 2. 분산 데이터베이스 시스템의 주요 목표
- 분산 데이터 독립성: 데이터베이스가 분산되어 있음을 사용자가 인식하지 못하게 하는 것
	- 분산 투명성 보장되어야 함
#### 위치 투명성
- 데이터베이스가 지역적으로 분산되어 있지만, 사용자가 접근하려는 데이터의 실제 저장 위치를 알 필요 없이 데이터베이스의 논리적인 이름만으로 데이터에 접근할 수 있다는 의미
- 물리적 위치를 알 필요가 없어져 응용 프로그램의 처리가 간단해짐
#### 중복 투명성
- 데이터 중복의 장단점

| 구분  | 내용                                                                                                                                                                                            |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 장점  | - 한 지역에서 문제가 발생해도 동일한 데이터가 저장되어 있는 다른 지역에서 작업을 계속 수행할 수 있으므로 신뢰성과 가용성이 높아짐<br>- 동일한 데이터가 저장된 여러 지역에서 병렬 처리를 수행할 수 있어 데이터 처리 성능이 향상됨<br>- 데이터 처리 요청이 한 지역에 집중되지 않고 여러 지역에 분산되므로 처리 부담을 줄일 수 있다 |
| 단점  | - 저장 공간 많이 사용<br>- 중복된 데이터도 모두 변경해야 하므로 비용 증가, 변경 도중에 문제가 생길 시 데이터 불일치 발생                                                                                                                     |
- 데이터 중복을 지원하는 분산 데이터베이스 시스템은 "중복 투명성" 제공해야 함
- 중복 투명성: 동일한 데이터가 여러 지역에 중복 저장되더라도 사용자가 중복을 인식하지 못하고, 하나의 데이터베이스 시스템에 데이터가 저장된 것처럼 사용하는 것
#### 단편화 투명성
- 단편화: 하나의 릴레이션을 더 작은 조각으로 나누고 각 조각을 별개의 릴레이션으로 처리하는 것
	- 각 조각이 전체 릴레이션의 일부가 되어 저장공간 적게 사용
	- 관리할 데이터 수 줄어듦
	- -> 데이터 중복 장점은 그대로, 단점은 보완
- 세 가지 조건 만족시켜야함
	- 완전성: 전체 릴레이션의 모든 데이터는 어느 한 조각에는 꼭 속해야 한다
	- 회복성: 단편화된 조각들로부터 원래의 전체 릴레이션을 회복할 수 있어야 한다
	- 분리성: 전체 릴레이션의 모든 조각을 서로 중복되지 않게 분리해야 한다
##### 수평적 단편화
- 릴레이션을 수평적으로 단편화
##### 수직적 단편화
- 릴레이션을 수직적으로 단편화
##### 혼합 단편화
- 수평적, 수직적 모두 사용
#### 병행 투명성
- 분산 데이터베이스와 관련된 트랜잭션들이 동시에 수행되더라도 결과는 항상 일관성을 유지
#### 장애 투명성
- 특정 지역 시스템에 문제가 발생하더라도 전체 시스템이 작업을 계속 수행할 수 있는 것
### 3. 분산 데이터베이스의 구조
- 전역 개념 스키마: 분산 데이터베이스에 저장할 모든 데이터 구조와 제약조건을 정의
- 단편화 스키마: 전역 개념 스키마를 분할하는 방법인 단편화를 정의함
- 할당 스키마: 각 조각 스키마의 인스턴스를 물리적으로 저장해야 되는 지역 정의
- 지역 스키마: 지역별로 저장하고 있는 데이터 구조와 제약조건 정의
### 4. 분산 데이터베이스의 질의 처리
- 최선의 질의 처리 전략을 선택하는 기준으로 데이터 베이스가 위치한 디스크에 접근하는 횟수 이용
### 5. 분산 데이터베이스 시스템의 장단점
#### 장점
- 신뢰성과 가용성 증대
- 지역 자치성과 효율성 증대
- 확장성 증대
#### 단점
- 추가로 고려할 사항이 많아 설계 및 구축 비용 더 듦
- 관리 복잡, 관리 비용 증가
- 통신 비용, 처리 비용 발생
## 4. 멀티미디어 데이터베이스 시스템
- 멀티미디어 데이터도 효과적으로 저장하고 처리하는 기능 함께 제공
### 1. 멀티미디어 데이터의 특성
- 멀티미디어 데이터: 여러 미디어의 조합으로 이루어진 데이터
- 특정
	- 대용량 데이터
	- 검색 방법이 복잡한 데이터
		- 설명 기반 검색
		- 내용 기반 검색
	- 복잡한 구조
### 2. 멀티미디어 데이터베이스의 발전 과정
#### 관계 데이터베이스에서의 멀티미디어 데이터 처리
- RDBS에 멀티미디어 데이터를 위한 새로운 데이터 타입 추가
- 하지만 일부 기능만 제공
#### 객체지향 데이터베이스에서의 멀티미디어 데이터 처리
- 객체지향 DB에서는 멀티미디어를 처리하는데 필요한 다양한 기능 제공
### 3. 멀티미디어 데이터베이스 관리 시스템의 구성
- 파일 시스템을 이용하는 방식
- 관계 데이터베이스 관리 시스템을 이용하는 방식
- 확장된 관계 데이터베이스 관리 시스템을 이용하는 방식
- 객체지향 데이터베이스 관리 시스템을 이용하는 방식
### 4. 멀티미디어 데이터의 질의
#### 질의 유형
- 텍스트 질의
- 이미지 질의
- 비디오 질의
- 공간 질의
#### 질의 처리
- 매칭 기법: 유사도 높은 데이터 검색
- 랭킹 기법: 관련 정도에 따라 정렬 후 높은 결과 제공
- 필터링 기법: 관련성 적은 데이터 제거하며 검색 범위 줄여가며 검색
## 5. 기타 데이터베이스 응용 기술
### 1. 웹 데이터베이스
- 웹 서비스와 데이터베이스 시스템을 통합
- 사용하려면 미들웨어 필요, 웹 서비스는 미들웨어를 통해 데이터베이스 시스템 기능을 제공 받음
	- 데이터베이스 통로 라고도 함
- 미들웨어를 구현하는 방법
	1. 미들웨어를 통해 데이터베이스에 접근하는 프로그램을 웹 서버 쪽에 두는 서버 확장 방법
	2. 클라이언트 쪽에 두는 클라이언트 확장 방법
### 2. 데이터 웨어하우스
- 수많은 데이터 중에서 의사 결정에 도움이 되는 데이터를 빠르고 정확히 추출할 수 있는 방법 중 하나
#### 주제 지향적
- 일반 DB는 업무 처리 중심의 데이터로 구성, 데이터 웨어하우스는 의사 결정이 필요한 주제를 중심으로 데이터 구성
#### 통합된 내용
- 내부적으로 데이터가 항상 일관된 상태를 유지하도록 여러 데이터베이스에서 추출한 데이터를 통합하여 저장
#### 시간에 따라 변하는 내용
- 각 시점의 데이터를 주기적으로 유지해 둠
	- 올바른 의사 결정을 위해
	- 각 시점의 데이터: 스냅샷
#### 비소멸성
- 검색 작업만 수행되는 읽기 전용의 데이터를 유지함
	- 정책에 따라 주기적으로 변경만 있고 트랜잭션 단위 변경은 없음
# Chapter 13. 데이터 과학과 빅데이터
## 1. 데이터 과학
### 1. 데이터 과학의 필요성
- 단순히 데이터 분류, 검색을 넘어 방대한 양의 데이터 속에서 규칙과 패턴을 찾아내 문제 해결에 활용하고 앞으로 벌어질 일을 예측하여 준비하기를 원함 -> 데이터 과학이 필요한 이유
### 2. 데이터 과학의 개념
- 데이터를 수집 후 분석을 통해 데이터를 정확히 이해함으로써 그 속에 숨겨진 새로운 지식을 발견하고 이를 문제 해결에 활용하는 모든 과정의 활동
- DIKW 계층 구조
	- 데이터: 관찰, 측정하여 수집한 사실이나 값
	-> 상황에 대한 이해
	- 정보: 데이터를 목적에 맞게 가공한 것
	-> 의미
	- 지식: 규칙과 패턴을 통해 찾아낸 의미 있고 유용한 정보
	-> 통찰력
	- 지혜: 지식에 통찰력을 더해 새롭고 창의적인 아이디어 도출
## 2. 빅데이터
### 1. 빅데이터의 개념
- 대규모의 데이터를 저장 및 관리하는 기술과 가치 있는 정보를 만들기 위해 분석하는 기술까지 포함하여 정의한 것
### 2. 빅데이터의 특징
- 3V로 요약
	- 데이터양: 테라바이트 단위 이상의 대량 데이터
	- 속도: 데이터 수집, 분석을 정해진 시간 내에 처리해야 함
	- 다양성: 정형, 반정형, 비정형 같은 다양한 형태의 데이터를 모두 포함
- 5V
	- 가치: 빅데이터 분석으로 얻은 결과는 문제 해결을 위한 의사 결정에 활용될 만한 유용한 가치를 지니고 있어야 함
	- 정확성: 가치 있는 결과를 만들려면 정확하고 신뢰할 수 있어야 함
- 7V
	- 시각화: 활용해야 하는 대상자가 결과 내용을 제대로 이해하지 못한다면 시간, 노력 의미 없음 -> 보기좋게 시각화하는게 좋다
	- 가변성: 빅데이터가 맥락에 따른 가변성을 가지고 있음을 인식하고 수집, 분석 작업에서 데이터의 원래 의미가 그대로 반영될 수 있도록 노력
### 3. 빅데이터의 기술
- 저장 기술
- 분석 기술
	- 텍스트 마이닝
	- 오피니언 마이닝
	- 소셜 네트워크 분석
	- 군집 분석
- 표현 기술
## 3. 빅데이터 저장 기술: NoSQL
### 1. NoSQL의 등장
### 2. NoSQL의 특징
- ACID를 위한 트랜잭션 기능을 제공하지 않는 대신, 저렴한 비용으로 여러 대의 컴퓨터에 데이터를 분산, 저장, 처리 가능한 데이터베이스
- 데이터 구조 미리 정의 필요없음
- 수시로 구조 변경 가능
	- 비정형 데이터 저장하기에 적합
### 3. NoSQL의 종류
#### 키-값 데이터베이스
- 어떤 형태의 값도 저장 가능
- 처리 속도 빠름
- 값 일부 검색, 값 내용을 이용한 질의 불가 -> 별도 처리 필요
#### 문서 기반 데이터베이스
- 트리 형태의 계층적 구조가 존재하는 JSON, XML 등과 같은 반정형 형태의 문서로 데이터 저장
- 문서 대상 질의 언어를 이용해 문서 내 일부 검색 및 질의에 활용 가능
#### 컬럼 기반 데이터베이스
- 관련 있는 컬럼 값들이 모여서 구성
- 다양한 형태 데이터를 값으로 저장 가능
- 행마다 컬럼 구성 댜르게 가능
#### 그래프 기반 데이터베이스
- 데이터 간의 관계 표현에 적합
- 노트에 데이터 정의, 간선으로 데이터 간 관계 표현
- ACID 지원
- 클러스터 환경 비적합
## 4. 빅데이터 분석 기술: 데이터 마이닝
### 1. 데이터 분석의 개념
- 수많은 데이터 속 유용한 정보, 지식을 찾아내기 위해 가공하는 역할
### 2. 기계 학습과 데이터 마이닝
- 데이터 마이닝: 분석 목적을 발견에 두는 것
- 기계 학습: 예측에 두는 것
### 3. 데이터 마이닝
#### 분류 분석
- 새로운 데이터가 어떤 그룹 또는 등급에 속하는지 판단
#### 군집 분석
- 미리 정해진 기준이 없는 상태에서 유사한 특성을 공유하는 데이터들을 여러 개의 독립적인 군집으로 나누는 것
#### 연관 분석
- 데이터 마이닝의 대표적인 분석 기법으로, 데이터 간의 발생 빈도를 분석하여 그 속에 숨겨진 연관 규칙을 파악
## 5. 빅데이터 표현 기술: 데이터 시각화
### 1. 데이터 시각화 개념
- 데이터 분석을 통해 추출한 결과를 모두가 이해하기 쉽고 보기 좋게 그림이나 그래프 등으로 표현
### 2. 데이터 시각화 방법
#### 시간 시각화
- 시간의 흐름에 따른 데이터의 변화나 경향을 시각적으로 표현
#### 분포 시각화
- 선택이나 분류 기준에 따라 전체적으로 데이터가 각 부분에 어떻게 분포되어 있는지 시각적으로 표현하는 방법
#### 관계 시각화
- 데이터 간에 어떤 관계가 있는지, 즉 어떤 상관관계가 있는지를 시각적으로 표현하는 방법이며 버블 차트, 산점도 등을 주로 활용
#### 비교 시각화
- 여러 항목의 데이터 값을 함께 비교해서 유사성이나 차이를 시각적으로 표현하기 위한 방법
#### 공간 시각화
- 지도 위에 데이터를 표시하여 장소나 지역에 따른 데이터의 분포를 시각화하는 방법
### 3. 데이터 시각화 도구

# 예상 질문
1. 분산 데이터베이스 시스템의 특징 중 한 가지와 왜 필요한지 간단히 설명해주세요
2. 데이터 분석의 필요성에 대해 간단히 설명 후 해본 프로젝트에서 데이터 분석을 추가해서 어떻게 기능적인 면을 고도화하고 싶은지 말해주세요.